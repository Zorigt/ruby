<!doctype html>
<html lang=en-us>
    <head>
        <meta charset=utf-8>
    <title>Lab 4 Example Output</title>
    <link rel="stylesheet" type="text/css" href="/~dputnam/assets/stylesheets/bootstrap.css" />
    <link rel="stylesheet" type="text/css" href="/~dputnam/assets/stylesheets/docs.css" />
    <link rel="stylesheet" type="text/css" href="/~dputnam/assets/stylesheets/app.css" />
    <script type="text/javascript" src="/~dputnam/javascripts/jquery.js" charset="utf-8"></script>

    <script type="text/javascript">
        $(document).ready(function(){
            $("button").click(function () {
            $("#source-code").toggle();
            $("#example-output").toggle();
            });
        });
    </script>
    <style>
       body {font-size: 18px;font-weight:300}
        table {border:solid 1px gray;font-size: 14px}
        th a {font-size: 14px;}
       /*
        th a:link {color:gold}
        th a:visited {color:lightblue}
        th a:link {color:gold}
        */
        pre {white-space:pre-wrap}
    </style>
    <link href='//fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600,700,900,300italic,400italic|Source+Code+Pro:400,500,600,700' rel='stylesheet' type='text/css'>
</head>
<body >
   <div class="row">
   <div class="container">
      <div class="col-md-12">
         <div class='page-header'>
   <h1>Lab 4 Example Output</h1>
</div>
   <p style="width: 800px;">
   This text is generated by ERB (Embedded Ruby). The number and file crunching
   took place in lab4.cgi and the presentation is accomplished in an ERB file.
   This technique is very similar 
   to how PHP, JSP, and ASP scripts process code embedded in text. Rails uses several
   types of templating, including ERB template. 
   </p>
   <% def humanize(string)
      string.gsub('_',' ').ucwords
      end
   %>
<button  class='btn btn-lg btn-primary' >Toggle Source/Output</button>
<!--
<h3>Profiling your scripts</h3>
<p>
To view the profile information, <a href="?crn=<%= @crn %>&profile=true">click here</a> then click the Toggle Source button.
-->
</p>
<br />
<br />

   <div id="example-output">
      <h1>Fall 2013 CS 132A Students</h1>
<table class="table table-striped">
   <tr>
   <% @fields.each do |f| %>
   <% if(@sort_by.to_s == f.to_s && @order.to_s == 'normal')
        @th_order = 'reverse'
      else
        @th_order = 'normal'
      end
   %>
         
   <% bgclass = (f.to_s == @sort_by ? ' class="th-selected" ': '') %>
   <th <%= bgclass %> ><a href="?sort_by=<%= f.to_s %>&amp;crn=<%= @crn %>&amp;order=<%= @th_order %>"><%= humanize(f.to_s)%></a></th>
   <% end %>
   </tr>
   <% @students_array.each do |student| %>
     <tr>
         <!--
     <tr style="background-color:#<%= bgcolor(student.number)%>;color:white">
         -->
      <% @fields.each do |f| %><% css = f.to_s == @sort_by ? 'highlight' : '' %> 
      <td class="<%= css %>"> <%= student.send(f) %> </td>
      <% end %>
   </tr>
   <% end %>
</table>

<%= @for_template %>
<hr />
</div>
<div id="source-code" style="display:none">
<%= @source_code %>
</div>
</div>
</body>
</html>